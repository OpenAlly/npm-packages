---
"@openally/servo": minor
---

Add a browser-safe `@openally/servo/paths` entry exporting `safePath`, `normalizePosix`, `isDotfilePath`, `decodeRequestPath` and the `SafePath` and `PathRejection` types without reaching any `node:` module.
